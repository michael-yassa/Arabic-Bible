<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <ImageView
        android:id="@+id/eshah_image"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintHeight_percent=".8"
        android:src="@drawable/eshahi"
        android:adjustViewBounds="true"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />
    <TextView
        android:id="@+id/ishah_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/eshah_image"
       app:layout_constraintEnd_toEndOf="@id/eshah_image"
        app:layout_constraintTop_toTopOf="@id/eshah_image"
        app:layout_constraintBottom_toBottomOf="@id/eshah_image"

        android:text="hjhjghjgj"
        android:gravity="center"
        android:textSize="15sp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>